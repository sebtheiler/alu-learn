import { Meta } from "@storybook/addon-docs";

<Meta title="pages/Index" />

# Pages

This directory contains the final pages displayed to the user. Everything here is directly rendered in Alu
through Next.js's routing.

The pro upgrade pages are currently broken in Storybook; however, they work fine in Alu's normal environment.
