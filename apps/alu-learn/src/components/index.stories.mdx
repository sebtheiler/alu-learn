import { Meta } from "@storybook/addon-docs";

<Meta title="components/Index" />

# Components

This directory is dedicated to custom reusable components that are built from various atoms.
Components in this directory should be simple and reusable, but should not be so simple that they are atoms.
